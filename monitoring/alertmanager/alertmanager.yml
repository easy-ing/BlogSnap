global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: [alertname, service, severity]
  group_wait: 5s
  group_interval: 2m
  repeat_interval: 4h

receivers:
  - name: default
    webhook_configs:
      - url: http://alert-webhook:5001/alerts
        send_resolved: true

global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: [alertname, service, severity]
  group_wait: 15s
  group_interval: 2m
  repeat_interval: 4h

receivers:
  - name: default
    webhook_configs:
      - url: http://localhost:5001/
        send_resolved: true
